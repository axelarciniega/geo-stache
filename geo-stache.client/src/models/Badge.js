export class Badge {
    constructor(data) {
        this.id = dta.id
        this.stacheId = data.stacheId
        this.accountId = data.accountId
        this.profile = data.profile
        this.stache = data.stache
        foundDate: date
        // NOTE add these in later when we have staches working
        // status: enum[started, completed..]
        // rating: enum [1, 2, 3, 4, 5]staches


    }
}